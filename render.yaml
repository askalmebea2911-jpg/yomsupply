services:
  - type: web
    name: yom-sales-sfa
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: SESSION_SECRET
        generateValue: true